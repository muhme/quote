<h1>Kennwort aktualisieren</h1>

<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= @user.errors.count %> Fehler</h2>
    <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
 
<p>Hier kannst das Kennwort für den Benutzernamen "<%= h(@user.login) %>" mit der E-Mail "<%= h(@user.email) %>" ändern.</p>
<p>Bitte das neue Kennwort zweimal eingeben.</p>
 
<%= form_tag password_reset_path, :method => :put do %>
  <%= hidden_field_tag(:login, @user.login) %>
  <%= label_tag(:password, "Neues Kennwort") %><br />
  <%= password_field_tag(:password, '', autofocus: true) %><br /><br />
  <%= label_tag(:password, "Neues Kennwort wiederholen") %><br />
  <%= password_field_tag :password_confirmation %><br /><br />
  <%= submit_tag "Kennwort aktualisieren" %>
<% end %>
