<h1><%= "#{number_with_delimiter(@quotations.total_entries)} #{t('g.quotes', count: @quotations.total_entries)} #{t(".quotations", category: @category.category)}" %></h1>
<%= render :partial => 'list_content' %>
