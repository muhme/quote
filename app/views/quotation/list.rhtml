<% pattern = h(params[:pattern]) %>
<h1>Zitate<%= " f&uuml;r \"#{pattern}\"" unless pattern.blank? %></h1>

<%= render :partial => 'list_content' %>

<% if logged_in? %>
    <p class="ListNavi">
    <% form_tag(:action=> 'new') do %>
      <%= submit_tag "Neu" %>
    <% end %>
    </p>
<% end %>
