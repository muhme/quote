<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login, id: :user_login %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :user_email %>
  </div>

  <div class="field">
    <%= form.label :admin %>
    <%= form.check_box :admin, id: :user_admin %>
  </div>

  <div class="field">
    <%= form.label :crypted_password %>
    <%= form.text_field :crypted_password, id: :user_crypted_password %>
  </div>

  <div class="field">
    <%= form.label :password_password_salt %>
    <%= form.text_field :password_salt, id: :user_password_salt %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
